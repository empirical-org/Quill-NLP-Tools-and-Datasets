runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

env_variables:
  GOOGLE_APPLICATION_CREDENTIALS: "grammar-api-c29ed2522250.json"
  SPACY_GRAMMAR_PATH: "models/spacy_grammar_mix/"

runtime_config:
  python_version: 3

# This sample incurs costs to run on the App Engine flexible environment.
# The settings below are to reduce costs during testing and are not appropriate
# for production use. For more information, see:
# https://cloud.google.com/appengine/docsp/flexible/python/configuring-your-app-with-app-yaml
instance_class: F4_1G
automatic_scaling:
  min_num_instances: 2
# resources:
#  cpu: 4
#  memory_gb: 4
#  disk_size_gb: 10
